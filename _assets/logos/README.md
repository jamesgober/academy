# Logos

Language logos used at the top of each language track page.

Add files here named after the language, e.g. `rust.svg`, `python.svg`. The
language track README references `../../_assets/logos/<lang>.svg`.

Prefer SVG. Keep originals; don't recolor official logos.
